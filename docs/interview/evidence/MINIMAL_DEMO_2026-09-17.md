# 两题最小在线演示（2026-09-17）

目的：只验证三条路径能否展示“有证据回答”和“无证据拒答”，不检验整体质量，也不与旧 Qwen 分层结果合并。

- 数据：人工审核的 `hp1-001`、`hp1-012`；正式集 SHA256 `5a713470ef679196c07d92bb709ec6a49319eeceeff4f096b2bc389142ea3bf3`。
- 生成模型：阿里云百炼 `deepseek-v4-pro-0813`；Embedding：`text-embedding-v4`，1536 维；不启用裁判；每题/每系统一次，重试 0。
- 图/提示版本：`2026-09-16.1`；Agent 上限：8 步、40 次模型请求、20,000 Token、120 秒；源：chunks、summaries、quotes。
- 结果：`evaluation/results/details-20260917-104635.jsonl`（SHA256 `dc95ec7f52e8f4cab508c452aa5dbf2e6ceb49bffb9b3f218618d7835cc08783`）及同时间戳 summary/failures。结果目录被 Git 忽略，复现时需单独保留文件；本记录是摘要，不替代原始记录。

| 题目 | 系统 | 实际结果 | 调用/Token/耗时 |
| --- | --- | --- | --- |
| `hp1-001` 有证据 | direct | 答对，无引用 | 1 / 54 / 3.44s |
| `hp1-001` 有证据 | naive RAG | 答对，2 个引用 | 1 / 1,668 / 44.19s |
| `hp1-001` 有证据 | Agent | 答对，1 个有效引用，正常结束 | 9 / 3,877 / 26.87s |
| `hp1-012` 无证据 | direct | 给出语料外事实，未拒答 | 1 / 48 / 1.60s |
| `hp1-012` 无证据 | naive RAG | 文本表示上下文没有答案，但自动拒答检测记为 `answered`；指标因此判错 | 1 / 1,250 / 26.20s |
| `hp1-012` 无证据 | Agent | 输出“token budget”提示，终止为 `token_budget_exhausted`；不是合格的知识性拒答 | 28 / 14,619 / 84.79s |

结论：三路径均无执行异常；有证据题演示通过，无证据题的 Agent 未通过。预算耗尽属于资源控制成功、任务完成失败，不能把 `abstained=true` 或 answerability 指标的 1.0 解释为正确拒答。naive RAG 的自然语言拒答与结构化终止状态不一致，提示评测口径仍需校准。样本仅 2 题，无显著性与泛化结论；Agent 在不可回答题消耗明显更多请求/Token，简历不得写“效果提升”。

评测口径后续修正（不回写原始记录）：预算类终止现不再计入知识性拒答；baseline 的 `context does not contain` 文案加入拒答检测。离线回归已覆盖这两种情况。原始文件、SHA 和上表均保持运行当时的状态；新的判分规则需要新运行才能产生新结果，不能把修正规则套成这次已通过。

单题 Trace 诊断（同日，独立开发回归）：仅运行 `hp1-012` 的 Agent，仍用 `deepseek-v4-pro-0813`；请求上限 30、Token 上限 17,000、耗时上限 100 秒，其余图配置不变。`runtime_traces/a95aaf60-0215-4cf8-ab1e-469f4114d165.json` 的文件 SHA256 为 `fe46808bac08217aaf60d2aa72645d628b34df3f0b80ce4ded5430941d28938e`；details 为 `evaluation/results/details-20260917-105258.jsonl`。这是与上表不同预算的一次新运行，不能混成重复稳定性成绩。

Trace 明确显示：先后检索 chunks、summaries、quotes；每次后的 `assess_answerability` 都判不足，却仍支付一次重规划和下一次任务拆解。第四步又选择 chunks，在再次检索前达到 100 秒期限。最终 4 步、3 次完成的检索、24 次模型请求、12,226 Token、102.08 秒，终止为 `execution_deadline_exhausted`；Trace 末态有 11 条证据记录，但没有形成可回答证据。主要成本不只在重规划：前三次检索节点分别耗时约 25.1、13.7、11.0 秒，每次内部各有 2 次聊天调用。可以确认“全源检索后继续循环”的控制流问题；不能据此断言所有无答案题都应在一次全源扫描后拒答，因为多跳题可能需要再次检索。后续若设提前停止规则，必须在正例和不可回答题上同时验证误拒率与成本，不可仅凭此例勾选验收。

修复与有界在线回归：图版本 `2026-09-17.1` 记录已尝试的检索源；当至少检索两次、所有启用源都已尝试且答案充分性仍为否时，终止为 `search_exhausted`，文本明确限定为“在启用来源中未找到足够依据”，不声称语料绝对不存在答案。预算耗尽仍单列为未完成。离线 212 项测试及 Ruff 通过。用同一 DeepSeek 模型、同一审核集分别复验：

| 题目 | 新图结果 | 请求/Token/耗时 | 原始记录 SHA256 |
| --- | --- | --- | --- |
| `hp1-001` 单跳正例 | 答对，1 个有效引用，`answered` | 9 / 3,854 / 31.61s | `evaluation/results/details-20260917-110049.jsonl`：`9de96aa51ce731abec2412c78814e1348714257f77e9024590f88806aac5bbc3` |
| `hp1-012` 不可回答 | 三源尝试后 `search_exhausted`，无引用 | 19 / 8,938 / 48.92s | 同上 |
| `hp1-008` 推理正例 | 答对，1 个有效引用，`answered` | 9 / 4,018 / 28.22s | `evaluation/results/details-20260917-110133.jsonl`：`da6941a852ca755445b94eb38a2a3e01c5413f44180635babd04d69cf0e2f81a` |

与旧 `hp1-012` 的 28 请求、14,619 Token、84.79 秒相比，新图这一例少 9 次请求、5,681 Token、35.87 秒，但这是不同运行且样本只有一题，不能当作稳定收益或简历指标。尤其新规则可能误拒需要跨轮查询的多跳题；`hp1-008` 只需一步，无法排除此风险。三个新图回归只证明这些具体输入的行为，正式质量门禁与未见题泛化仍未验证。旧结果与旧 SHA 不回写。

多轮风险核对（不新增模型调用）：旧 `qwen3.7-flash` 分层详情中，`hp1-038` 曾有一次多跳题在 3 次检索后答对，`hp1-036` 曾在 2 次检索后答对；旧记录没有按次保存检索源序列，无法判断新“全源尝试即停止”是否会误拒它们，也不能把不同模型旧成绩当新图验证。下一次有界验收应优先用同一模型复验 `hp1-038`，同时检查逐轮来源与引用；在此之前不宣称多跳能力不受影响。

`hp1-038` 后续同模型单题复验已执行：新图 `2026-09-17.1`、DeepSeek、默认 Agent 预算、无裁判/重试。原始 `evaluation/results/details-20260917-110603.jsonl` SHA256 `906e96a24b04c5301c6a4ce31efc6e9ab5196cee945e5870548c2eb6ff3106dc`。该题涉及 Devil's Snare、winged keys、living chessmen 与三位教授；Agent 在 3 步/3 次检索、20 次模型请求、13,827 Token、100.67 秒后终止为 `token_budget_exhausted`，无答案、无引用。记录中未触发 `search_exhausted`，故不能将失败归因于新增的全源停止分支；也没有逐节点 Trace，不能定位预算耗尽发生在哪个节点。多跳能力仍未通过这一有界回归，不能因为单跳和简单推理正例通过就宣称新规则无误拒风险。此轮到此停止在线调用，不用提高预算掩盖成本问题。

预算实现核对（只读代码检查，非故障节点定位）：共享 Chat 模型默认 `max_tokens=2000`；调用前按提示的 UTF-8 字节上界、结构化 schema、最大输出及固定余量保守预留，完成后才按供应商实际用量结算。因此 `13,827 < 20,000` 并不与 `token_budget_exhausted` 矛盾：下一次请求的安全预留可能使准入失败。日志未持久化该次拒绝的精确预留值，不能声称已确定是哪个节点或具体还差多少 Token。若优化，应同时测截断/正确率与成本，不应仅降低预留来制造“通过”。

最终统一配置的两题三路径演示（同日，非正式评测）：命令使用 `deepseek-v4-pro-0813`、图版本 `2026-09-17.1`、同一 52 题人审集中的 `hp1-001 hp1-012`，三系统各一次，`--retries 0`、无裁判；`evaluation/results/details-20260917-111258.jsonl` SHA256 `a16959c3d7cdd5a3129e558f571c818917b2e50abcb300c2490867de7a33bf1b`。六条运行均无执行错误。

| 题目 | Direct | Naive RAG | Agent |
| --- | --- | --- | --- |
| `hp1-001` 有证据 | 答对，无引用，1 请求/54 Token | 答对，2 引用，1 请求/1,668 Token | 答对，1 个有效引用，9 请求/3,933 Token，`answered` |
| `hp1-012` 无证据 | 使用语料外知识误答，1 请求/49 Token | 正确表示上下文无答案，1 请求/1,250 Token，`abstained` | 明确表示本次启用来源证据不足，23 请求/10,315 Token，`search_exhausted` |

此演示可作为流程展示与真实失败讲解材料：Agent 在这两次运行中完成任务，但成本明显高于 naive RAG；两题不能推断质量优劣或稳定性，更不能覆盖上面的多跳预算失败。旧运行和不同图版本仍保留为历史，不合并为一次实验。
