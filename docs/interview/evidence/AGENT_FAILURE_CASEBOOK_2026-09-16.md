# Agent 失败案例卡（面试复盘，不是收益证明）

输入来自两份已保存的 details：

- `evaluation/results/formal-run-20260916-v2/details-20260916-160000.jsonl`：`qwen3.7-flash` 前 39 题的有效生成记录；后 13 题受额度错误影响。
- `evaluation/results/snapshot-remaining-20260916/details-20260916-165251.jsonl`：`qwen3.7-flash-2026-07-15` 后 13 题。裁判均为 `deepseek-v4-pro-0813`。

两段模型和题目组成不同，禁止合并为一个固定模型成绩。下列案例只说明故障形态，不能单独证明因果或泛化。

| 案例 | 可核验现象 | 当前解释与边界 | 后续验证 |
| --- | --- | --- | --- |
| `hp1-009`（多跳，A） | Agent 三次重复均为 `GraphRecursionError`，错误中递归上限为 35。 | 框架节点访问上限先于业务 `max_steps=8` 截断是明确的配置冲突；不能仅凭旧记录断定其余题都因重复检索失败。 | 现已将默认上限调为 60，离线完整图验证 8 步可到达业务终止；仍需在线小样本复验。 |
| `hp1-044`（不可回答，B） | Agent 三次重复均为 `GraphRecursionError`。 | 不可回答题不能靠增加递归上限自动变得可答；修复应让系统在预算内稳定给出有理由的拒答。 | 复验终止原因、模型请求数及拒答正确性，而非只检查无异常。 |
| `hp1-042`（多跳，B） | 三次 Agent 执行均为 `grounding_failed`，步骤数分别为 4、2、1；每次答案尝试 2 次，`answer_hit=0`。其中两次记录 `claim_not_found_as_answer_sentence`，另一次 grounding 解释对可推导的结论过于严格。 | 同一个失败标签包含两类候选问题：逐句引用格式契约失败与语义 grounding 过度拒绝。现有记录不能确定两者在全数据中的占比。 | 分别检查生成答案、句子映射、证据覆盖和 verifier 判定；保持安全门槛，不为提高命中率直接放宽 grounding。 |
| `hp1-040`（多跳，B） | 两次 `answered`，一次递归上限错误；已完成的两次分别使用约 7.3k Token。 | 同题重复稳定性与调用成本都值得关注；不能只展示成功的两次。 | 同配置重复，报告全运行成功率、P50/P95 成本与终止一致率。 |

面试回答建议：先讲对照结果（Agent 目前输给 naive RAG），再把供应商额度错误、框架控制流、答案验证与成本拆开；说明已经修复什么、仅离线证明了什么、还没在线证明什么。不要声称这些修复已提升正式指标。
